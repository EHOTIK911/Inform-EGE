# Для начала создаем функцию. к примеру назовем её task15 , и присвоим значения : x и A.
def task15(x,A):
    # Далее напишем само выражение.
    return (x & 30 == 0) or ((x & 57 == 0) <= (x & A != 0))
# Заведем переменную A
for A in range (1,1000): # Большое значение смысла вводить нету, т.к. во всех заданиях мы встречаем перебор маленьких чисел,
                         # а от большого значения программа будет дольше работать.
    Flag = True # Так же создаем переменную в виде флага (Не обязательно Flag).
    for x in range(1,1000):  # Аналогично как и с "A".
        if task15(x,A) == False: # если наша функция ложна, то присваиваем Flag = False.
            Flag = False
            break # Возвращаемся к начальной стадии Flag = True
    if Flag == True: # " == True " можно не писать, т.к. в строке 5  мы уже присвоили значение Flag True!
        print(A)

#ответ: 6.
